# SQL Server (Windows Authentication, SQLEXPRESS)
DATABASE_URL="sqlserver://localhost\\SQLEXPRESS;database=Gradebook;integratedSecurity=true;trustServerCertificate=true"

# SQL Server (логин sa) — раскомментируй и укажи пароль
# DATABASE_URL="sqlserver://localhost:1433;database=Gradebook;user=sa;password=YourPassword123;encrypt=true;trustServerCertificate=true"

# LocalDB
# DATABASE_URL="sqlserver://(localdb)\\MSSQLLocalDB;database=Gradebook;integratedSecurity=true;trustServerCertificate=true"

JWT_SECRET="gradebook-dev-secret-change-in-production"
PORT=3001
